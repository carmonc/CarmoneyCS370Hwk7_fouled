<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/configPrefs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="21dp"
        android:text="Configure Preferences"
        android:onClick="configPreferences"  />

    <Button
        android:id="@+id/getAssignments"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/configPrefs"
        android:layout_below="@+id/configPrefs"
        android:layout_marginTop="27dp"
        android:text="Retrieve assignments list from server" />

    <Button
        android:id="@+id/manageAssignments"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/getAssignments"
        android:layout_below="@+id/getAssignments"
        android:layout_marginTop="22dp"
        android:text="Manage Assignments" />

</RelativeLayout>